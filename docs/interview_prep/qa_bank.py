# -*- coding: utf-8 -*-
"""Interview Q&A bank for the AI Invoice & Expense Manager.

Each entry: question, short (one-line) answer, detailed answer, a spoken
delivery version, and a likely follow-up (question + short answer).
Every question here is answerable from the actual implementation — none
of these require knowledge of a technology this project doesn't use.
"""

QA_BANK = {
    "Project-Specific": [
        {
            "q": "Walk me through what happens end-to-end when a user uploads a receipt.",
            "short": "Validate -> OCR -> extract fields -> normalize merchant -> suggest category -> show for verification -> confirm -> analytics.",
            "detailed": "The file is validated for type/size/corruption, then loaded as an image (PDF pages via pdf2image). It goes through OpenCV preprocessing and Tesseract OCR to produce raw text. That text is cleaned and run through regex extraction for merchant, amount, date, currency, invoice number, tax, subtotal, and payment method. The merchant name is normalized, and a category is suggested by the rule-based categorizer (with an optional ML second opinion). All of this is inserted into SQLite as a 'pending' transaction. The verification screen shows every field, editable, and only an explicit 'Confirm Transaction' click marks it confirmed — which is the only state analytics reads.",
            "verbal": "I'd walk it stage by stage the way it's laid out in app/ — validation, OCR, extraction, normalization, categorization, then the verification gate before anything counts as real data.",
            "follow_up_q": "What happens if OCR fails completely?",
            "follow_up_a": "process_upload returns a result with success=False and a specific error message ('OCR could not read any text from this file') — nothing gets inserted into the database at all.",
        },
        {
            "q": "Why did you build the human verification step? Isn't that extra friction?",
            "short": "Because OCR and regex extraction are both imperfect, and a financial dashboard built on unverified numbers isn't trustworthy.",
            "detailed": "It is extra friction, and I decided that tradeoff is worth it specifically because this is a financial tool. If I let raw extraction go straight into 'confirmed' data, a single misread amount would silently corrupt the dashboard, and the user would have no way to know which numbers to trust. The verification screen also directly surfaces the extraction_confidence score and flags amounts that came from a fallback guess rather than a labeled total, so the user knows exactly when to scrutinize a field more carefully.",
            "verbal": "I'd frame it as a product decision I made deliberately, not something I'd cut if I were rebuilding this — it's the one thing in the architecture that exists specifically because it's a financial app.",
            "follow_up_q": "Could you make verification optional for users who trust the extraction?",
            "follow_up_a": "You could add a 'high-confidence auto-confirm' toggle for extractions above some confidence threshold, but I'd be cautious about it — it reintroduces the exact risk the gate exists to prevent, just for a subset of cases.",
        },
        {
            "q": "Why is the rule-based categorizer the default instead of the ML classifier?",
            "short": "Transparency, no training-data dependency, and the ML model's honest evaluation doesn't support treating it as production-ready.",
            "detailed": "A rule firing is traceable — you can look at rules.py and know exactly why a transaction got its category. The ML classifier's decision, while inspectable in principle, isn't something a user can reason about from the UI. The classifier is also trained on 240 synthetic, templated examples; its ~98% test accuracy reflects that the synthetic categories use distinct vocabulary by construction, not that it would generalize that well to messy real receipts. I didn't want to present that as production-ready, so it's shown as a secondary suggestion instead.",
            "verbal": "I'd be upfront that this was a judgment call about trustworthiness, and cite the actual measured accuracy and why I don't fully trust it yet.",
            "follow_up_q": "What would make you switch the default to the ML model?",
            "follow_up_a": "Enough real confirmed-transaction history to retrain it on actual data instead of synthetic templates, plus evaluating it against real receipts specifically, not just a held-out split of the synthetic set.",
        },
        {
            "q": "What was the hardest bug you ran into building this?",
            "short": "A date parsing bug where dateutil's dayfirst=True flag mis-parsed unambiguous ISO dates, and a false 'amount' extraction where a bill number got picked up as a ₹2 billion charge.",
            "detailed": "I tested the extraction pipeline against real synthetic receipts I generated, and found dateutil.parser.parse('2026-08-05', dayfirst=True) returns May 8th instead of August 5th — dayfirst apparently still reorders the trailing two fields even when the year is unambiguously first. I fixed it by parsing year-first ISO dates directly with datetime() instead of trusting dateutil's flag. Separately, on a synthetic electricity bill with bill number 'EB2026080099' and no cleanly labeled total match, the largest-number fallback picked up the digits inside that bill number as a ₹2,026,080,099 amount. I fixed it with a regex negative-lookaround so a number can only count as an amount if it isn't glued to a letter or another digit.",
            "verbal": "I'd tell this one concretely — it's a good story because it shows I actually ran the pipeline against real inputs rather than just trusting the code looked right.",
            "follow_up_q": "How did you catch that bug?",
            "follow_up_a": "I ran the full pipeline against the synthetic sample receipts and printed every extracted field for manual inspection — the ₹2 billion number was obviously wrong at a glance, which is exactly why I test against realistic inputs rather than only unit tests with hand-picked strings.",
        },
        {
            "q": "How would this system scale to thousands of users?",
            "short": "It wouldn't, as built — SQLite is single-writer and there's no auth; I'd need Postgres, a real API layer, and background OCR processing.",
            "detailed": "This is explicitly a single-user local prototype. SQLite handles one writer at a time, which is fine for one person clicking through a Streamlit app but not concurrent multi-user writes. I'd move to Postgres (the repository.py interface wouldn't need to change much, just the underlying SQL dialect), add authentication and per-user data isolation, and move OCR off the request path into a background job queue (OCR on a multi-page PDF can take a few seconds, which is fine synchronously for one user but wouldn't scale to concurrent uploads).",
            "verbal": "I'd answer this directly and not pretend the current architecture already scales — the honest answer is more credible than a vague 'it would scale fine.'",
            "follow_up_q": "Why not just start with Postgres and a real API from the beginning?",
            "follow_up_a": "Because the project's actual requirement was one user, learning the pipeline end-to-end — adding infrastructure for a scale problem I didn't have would have been exactly the kind of over-engineering the brief for this project explicitly warned against.",
        },
        {
            "q": "What would you do differently if you rebuilt this from scratch?",
            "short": "Design the corrections-as-training-data loop from day one, and get real (even a handful of anonymized) receipts to test extraction against instead of only synthetic ones.",
            "detailed": "Every confirmed edit already updates the category column in the database, but I didn't wire up retraining the classifier from that data — I'd design that loop in from the start next time. I'd also try to get even a small set of real, anonymized receipts (with permission) to validate extraction against, because synthetic receipts are cleaner and more predictable than real photos, and my confidence in the extraction heuristics is really only tested against my own synthetic generator's output.",
            "verbal": "This is a good one to answer honestly with a specific, technical improvement rather than something vague like 'I'd add more features.'",
            "follow_up_q": "Isn't using synthetic data for both training and testing circular?",
            "follow_up_a": "Yes, for the ML classifier specifically — that's exactly why docs/ML_METRICS.md is explicit that the high accuracy reflects the synthetic data's distinct vocabulary, not real-world generalization.",
        },
        {
            "q": "Why Streamlit instead of a React frontend with a REST API?",
            "short": "One user, one process — Streamlit lets the whole app be Python, which matched the actual requirement without adding a second language and a network boundary.",
            "detailed": "A REST API plus a separate frontend makes sense when you need a different team working on UI, multiple client types, or independent scaling of frontend and backend. None of that applied here. Streamlit let me build upload, verification, dashboard, and query pages that call the app/ packages as direct Python function calls — no serialization boundary, no separate deployment, and the whole codebase stays in one language, which also made it easier to keep the project at a scope I could fully understand and defend.",
            "verbal": "Frame it as a deliberate scope decision, and be ready to say what WOULD justify the heavier architecture.",
            "follow_up_q": "What would push you toward a separate API layer?",
            "follow_up_a": "A mobile app client, multiple frontend surfaces, or needing to scale the OCR/analytics backend independently of the UI — none of which this project's actual requirements included.",
        },
        {
            "q": "How does editing a transaction in the table affect the dashboard?",
            "short": "Immediately — edits go through the same repository.update_transaction function analytics reads from, so there's no separate recalculation step.",
            "detailed": "The transaction table (app/ui/transactions_page.py) uses Streamlit's data_editor, diffs the edited DataFrame against the original, and calls update_transaction() for any row with changed values. Because the dashboard and analytics always query the current state of the transactions table fresh on every page load, an edit is reflected the next time the dashboard re-renders — there's no cache or materialized view to invalidate.",
            "verbal": "Good place to mention there's no caching layer — simpler, and correct for this scale, but worth naming as a design choice.",
            "follow_up_q": "Would that approach work if the dashboard had to handle a huge transaction history?",
            "follow_up_a": "At some point you'd want to cache aggregates rather than recomputing pandas groupbys over the full table on every page load — not needed yet at this project's scale, but the first place I'd optimize if it became slow.",
        },
        {
            "q": "What's the confirmation_status field actually enforcing, mechanically?",
            "short": "Every analytics/query function is called with status='confirmed' as an explicit filter — pending rows are structurally excluded, not just hidden in the UI.",
            "detailed": "repository.list_transactions(status='confirmed') builds a SQL WHERE clause on confirmation_status. Every call site in app/ui/dashboard_page.py, transactions_page.py, and query_page.py explicitly passes status='confirmed'. So it's not that pending transactions are filtered out visually — they're never even fetched from the database for analytics purposes. The only way a row moves from pending to confirmed is repository.confirm_transaction(), which is only called from the verification screen's Confirm button.",
            "verbal": "Point to the exact code path if asked to be specific — this is the kind of question where citing the actual function name shows you know the codebase, not just the concept.",
            "follow_up_q": "Could a bug elsewhere in the code accidentally show pending data on the dashboard?",
            "follow_up_a": "Only if a new call site forgot to pass status='confirmed' — there's no global filter enforced at the database layer, which is a real gap; a stricter design would default list_transactions() to confirmed-only unless explicitly overridden.",
        },
        {
            "q": "Why does insights.py sometimes generate fewer insights than expected?",
            "short": "Because it only generates an insight when the underlying comparison is actually possible with real data — no data for last month means no month-over-month sentence.",
            "detailed": "generate_insights() checks preconditions before generating each sentence — e.g. _month_over_month_insights returns an empty list if either the current or previous month's DataFrame is empty, rather than fabricating a comparison against zero. This was a deliberate choice: a fake 'infinite % increase' or a comparison against no data would look like a real insight but wouldn't be one.",
            "verbal": "This is a good example to have ready of 'graceful degradation over fabrication' as a principle running through the whole project.",
            "follow_up_q": "What does the app show if there's no data at all yet?",
            "follow_up_a": "generate_insights() returns a single explicit message: 'No confirmed transactions yet — insights will appear once you confirm some receipts.'",
        },
    ],
    "OCR": [
        {
            "q": "Why Tesseract instead of a cloud OCR API like AWS Textract or Google Document AI?",
            "short": "Runs fully offline with no API key or billing, which matters for a project meant to be cloned and run by anyone evaluating it.",
            "detailed": "Cloud OCR services generally outperform Tesseract on harder documents (rotated text, complex layouts, handwriting), and I say that directly rather than pretend Tesseract is state-of-the-art. But this project's inputs are printed receipts, which is within Tesseract's strengths, and requiring an interviewer or grader to sign up for a cloud account and pay for API calls just to run the project would undermine the whole point of a portfolio piece being easy to actually try.",
            "verbal": "Be honest that this is a tradeoff, not a claim that Tesseract is objectively best — that honesty is more convincing than overselling it.",
            "follow_up_q": "How would you add a cloud OCR fallback?",
            "follow_up_a": "app/ocr/engine.py's interface is run_ocr_with_confidence(image) -> (text, confidence) — a cloud provider implementation behind the same interface, selected by an environment variable similar to how app/llm/factory.py picks an LLM provider, would slot in without changing any other layer.",
        },
        {
            "q": "What preprocessing steps do you apply before OCR, and why each one?",
            "short": "Grayscale, upscale if small, denoise, adaptive threshold — each addresses a specific real problem with phone-photo receipts.",
            "detailed": "Grayscale removes color information that doesn't help text recognition. Upscaling (if the image is under 1000px on its longest side) compensates for messaging apps compressing photos below the resolution Tesseract's LSTM models expect. Denoising (cv2.fastNlMeansDenoising) removes sensor grain from low-light phone photos that can look like extra character strokes. Adaptive thresholding binarizes the image using a locally computed threshold per region rather than one global cutoff, which matters because receipt photos often have uneven lighting — a shadow across half the paper would blow out under a single global threshold.",
            "verbal": "Name all four and be ready to explain any one of them in more depth if asked — this is one of the questions most likely to get a 'why' follow-up on a specific step.",
            "follow_up_q": "Why adaptive thresholding instead of a fixed global threshold?",
            "follow_up_a": "A global threshold assumes uniform lighting across the whole image — a real receipt photo often doesn't have that, and adaptive thresholding computes the black/white cutoff locally so uneven lighting doesn't blow out half the image.",
        },
        {
            "q": "What does --psm 6 mean, and why did you choose it?",
            "short": "Tesseract's page segmentation mode; 6 means 'assume a single uniform block of text,' which fits a receipt better than the default layout-detection mode.",
            "detailed": "Tesseract's default (--psm 3) tries to detect a page layout — headers, columns, etc. — which is built for documents like scanned pages of a book or a form. A receipt is a single dense narrow column of text with no real layout to detect, and --psm 6 performed better on the sample receipts used during development because it doesn't waste effort trying to segment a layout that isn't there.",
            "verbal": "Good to mention this is a real, tested tradeoff — --psm 6 would perform worse on an actual multi-column layout, which I say explicitly in docs/OCR_PIPELINE.md.",
            "follow_up_q": "What page segmentation mode would you use for a different kind of document, like a form?",
            "follow_up_a": "Something closer to the default --psm 3, or a mode aware of sparse text / specific regions, since a form has real structure Tesseract needs to detect rather than assume.",
        },
        {
            "q": "What's the difference between the two confidence scores in this codebase?",
            "short": "OCR confidence is Tesseract's own per-word character-reading confidence; extraction confidence is a hand-written heuristic about whether the right fields were found.",
            "detailed": "run_ocr_with_confidence() in app/ocr/engine.py returns Tesseract's mean per-word confidence (0-100), which reflects how sure the OCR engine is about the characters it read — it says nothing about which word is the merchant vs. an address line. Separately, _estimate_confidence() in app/extraction/fields.py is a heuristic 0-1 score based on whether a labeled total was found (vs. guessed), and whether a date and merchant were found at all. It's not a calibrated statistical probability — it exists purely to decide when the verification screen shows an extra warning.",
            "verbal": "This distinction is worth being crisp about — conflating the two would be a real mistake to make out loud in an interview.",
            "follow_up_q": "Could you combine them into one confidence score?",
            "follow_up_a": "You could, but I kept them separate because they measure genuinely different things — character-level OCR certainty versus field-level extraction completeness — and collapsing them into one number would hide which kind of uncertainty you're looking at.",
        },
        {
            "q": "What are the known OCR failure cases in this project?",
            "short": "Blurry/low-light photos, handwritten receipts, non-standard label phrasing, and multi-page PDFs beyond page one.",
            "detailed": "Blurry or low-light photos produce noisy text that can defeat the labeled-total regex matching, forcing a fallback to the largest-number guess. Handwriting isn't something Tesseract's printed-text-trained LSTM models handle. A receipt using label phrasing not in my TOTAL_LABELS list (e.g. an unusual abbreviation) won't be recognized as a labeled total. And I only OCR the first page of a PDF — a total on page 2 of a multi-page document wouldn't be found.",
            "verbal": "Have these four ready as a list — this question comes up often and rewards a complete, specific answer over a vague one.",
            "follow_up_q": "How would you detect that an extraction is unreliable rather than just wrong?",
            "follow_up_a": "That's exactly what extraction_confidence and amount_source are for — a fallback ('largest_amount' instead of 'labeled_total') is a structural signal the value might be wrong, surfaced to the user rather than hidden.",
        },
        {
            "q": "Why do you only process the first page of a PDF?",
            "short": "Receipts are almost always single-page; a multi-page PDF is more likely a statement, which this pipeline isn't built to segment.",
            "detailed": "MAX_PDF_PAGES_PROCESSED = 1 in app/ocr/pipeline.py is an explicit, documented limitation, not an oversight. Supporting multi-page documents well would mean deciding which page(s) contain the actual transaction total, which is a different and harder problem than single-receipt extraction — I scoped it out rather than build a half-working version of it.",
            "verbal": "Frame this as a scoping decision you made consciously, with the reasoning ready, not as something you forgot to handle.",
            "follow_up_q": "How would you extend it to handle multi-page documents?",
            "follow_up_a": "OCR every page, then either let the user pick which page has the total, or extend the total-detection heuristic to search across all pages and take the most confident labeled match found anywhere in the document.",
        },
        {
            "q": "What's the difference between OCR and OCR post-processing in your pipeline?",
            "short": "OCR (Tesseract) turns pixels into text; everything after that — cleaning, then regex extraction — is post-processing that turns text into structured data.",
            "detailed": "app/ocr/ is strictly image-to-text: preprocessing an image and calling Tesseract. app/extraction/ is a separate concern — cleaning.py normalizes whitespace/line breaks in the raw OCR output, and fields.py runs regex heuristics over that cleaned text to pull out specific fields. Keeping them as separate packages means I can test extraction logic against hand-written text fixtures without needing an image or Tesseract at all (see tests/test_extraction.py) — the tests don't require Tesseract to be installed.",
            "verbal": "This is a good architecture question to connect back to testability, not just describe the split abstractly.",
            "follow_up_q": "Could extraction ever run without OCR at all?",
            "follow_up_a": "Yes — if the input were already digital text (a text-based PDF instead of a scanned image), you could skip OCR and feed extraction directly, though that path isn't built here since the product brief assumed photographed/scanned receipts.",
        },
        {
            "q": "How would you measure OCR accuracy properly, if you had to?",
            "short": "Character error rate or word error rate against a labeled ground-truth transcription of real receipts — which I don't have, so I haven't claimed an accuracy number.",
            "detailed": "The standard approach is comparing OCR output against a manually transcribed 'ground truth' for a labeled set of real documents, computing character error rate (CER) or word error rate (WER) via edit distance. I don't have a labeled real-receipt dataset for this project, so docs/OCR_PIPELINE.md and docs/LIMITATIONS.md are explicit that no formal OCR accuracy number is claimed — I only report Tesseract's own per-word confidence, which is a different thing entirely.",
            "verbal": "This is a chance to show you know the correct methodology even though you didn't have the data to run it — say both parts.",
            "follow_up_q": "Why not just report Tesseract's confidence score as an accuracy number?",
            "follow_up_a": "Because confidence and correctness aren't the same thing — Tesseract can be confidently wrong, especially on a clean-looking misread — so I keep them explicitly labeled as different things rather than implying one measures the other.",
        },
        {
            "q": "Does the OCR pipeline work on non-English receipts?",
            "short": "Not currently — the code assumes English (Tesseract's default language model) and hasn't been tested with other languages.",
            "detailed": "Tesseract supports multilingual OCR via language packs (e.g. --lang hin+eng for Hindi+English), but app/ocr/engine.py hardcodes the default English model, and none of the regex label matching in extraction (fields.py) accounts for non-English labels. This is a real, stated future improvement, not something the current code handles.",
            "verbal": "Direct, honest answer — don't imply it works when it doesn't.",
            "follow_up_q": "What would multilingual support require?",
            "follow_up_a": "Installing the relevant Tesseract language packs, passing --lang to pytesseract, and extending the extraction label lists (TOTAL_LABELS, TAX_LABELS, etc.) with equivalents in each supported language.",
        },
    ],
    "ML & Classification": [
        {
            "q": "Why TF-IDF plus Logistic Regression instead of a neural network?",
            "short": "With ~240 short synthetic examples, a neural network would overfit before learning anything generalizable, and would be harder to inspect than logistic regression's per-feature weights.",
            "detailed": "Model complexity should match data size. A dataset this small can't support a model with enough capacity to meaningfully outperform a simpler one — a neural net would likely just memorize the 240 training examples' specific phrasing. Logistic regression over TF-IDF features is also directly inspectable: you can look at which words have the highest learned weight for each category, which matters when I'm being honest with myself (and an interviewer) about whether the model has learned something sensible or just latched onto a coincidental pattern.",
            "verbal": "This is a chance to show engineering judgment about matching model complexity to data size, not just knowing that logistic regression exists.",
            "follow_up_q": "At what dataset size would a neural network become justified?",
            "follow_up_a": "There's no fixed number, but you'd want at minimum several thousand real, diverse examples before the extra capacity of a neural network is likely to help rather than just overfit — and even then, I'd want a good baseline like this one to know whether the added complexity was actually earning its keep.",
        },
        {
            "q": "Explain precision, recall, and F1 in the context of your classifier.",
            "short": "Precision: of transactions predicted 'Food,' how many actually were. Recall: of transactions that actually were 'Food,' how many got predicted correctly. F1 is their harmonic mean.",
            "detailed": "For a specific category like Food: precision = true positives / (true positives + false positives) — how trustworthy a 'Food' prediction is. Recall = true positives / (true positives + false negatives) — how many actual Food transactions the model actually catches. I report macro-averaged precision/recall/F1 (each category weighted equally regardless of how many test examples it has), rather than micro-averaged, specifically so a category with fewer synthetic examples isn't drowned out in the average.",
            "verbal": "Have a one-sentence definition of each ready, and be ready to explain macro vs. micro averaging if pushed further.",
            "follow_up_q": "Why macro-average instead of micro-average?",
            "follow_up_a": "Micro-averaging would weight by class frequency, so with an even 20-per-category synthetic dataset it wouldn't actually differ much here — but macro-averaging is the more honest default in general because it doesn't let a dominant class hide poor performance on smaller ones.",
        },
        {
            "q": "What does the confusion matrix tell you that accuracy alone doesn't?",
            "short": "Which specific categories get confused with which — accuracy is one number; the confusion matrix shows the error pattern.",
            "detailed": "docs/ML_METRICS.md's confusion matrix shows, for example, exactly 1 Groceries example misclassified as Shopping in the test run (everything else was classified correctly). A single accuracy number wouldn't tell you that the model's one error was a reasonable near-miss (groceries and general shopping share some vocabulary) rather than something bizarre like confusing Food with Rent, which would suggest a real problem with the features.",
            "verbal": "Cite the actual measured result from this project (Groceries/Shopping) rather than speaking generically — it shows you looked at your own output.",
            "follow_up_q": "Is a Groceries/Shopping confusion concerning?",
            "follow_up_a": "Not especially — those categories can have genuinely overlapping vocabulary (e.g. 'Amazon' could plausibly appear in either context), so a small amount of confusion between adjacent categories is a reasonable, expected kind of error rather than a sign the model learned nothing.",
        },
        {
            "q": "Why is your classifier's accuracy around 98%, and should I trust that number?",
            "short": "No — it's measured honestly but on synthetic, templated data with fairly distinct per-category vocabulary, so it overstates real-world performance.",
            "detailed": "The 98% is a real, reproducible number from a genuine held-out test split (not training accuracy, not fabricated) — but the synthetic dataset was generated from templates where each category draws from a fairly distinct vocabulary pool by construction (Food templates always include words like 'Biryani'; Utilities templates always include 'electricity bill'). A classifier finds that easy to separate. It says the pipeline works end to end, not that it would perform this well on a messy real receipt for an unfamiliar merchant. I say this explicitly in docs/ML_METRICS.md rather than let the number speak for itself.",
            "verbal": "This is one of the most important questions to answer well — showing you understand WHY a good metric doesn't automatically mean a good model is exactly the kind of judgment this question is testing for.",
            "follow_up_q": "How would you get a trustworthy accuracy number?",
            "follow_up_a": "Evaluate against real, held-out receipts the model has never seen anything similar to during training — ideally collected independently of whatever process generated the training data, so there's no shared artificial pattern inflating the score.",
        },
        {
            "q": "What features does the classifier actually use?",
            "short": "TF-IDF vectors (unigrams + bigrams) over the combined merchant name and OCR text.",
            "detailed": "TfidfVectorizer(ngram_range=(1,2)) in app/classification/ml_classifier.py converts the input text into weighted word/word-pair frequency vectors — words that are distinctive to a document relative to the whole corpus get higher weight than words that appear everywhere. Bigrams (two-word sequences) capture some phrase-level signal a pure unigram model would miss, like 'electricity bill' as a unit rather than just 'electricity' and 'bill' separately.",
            "verbal": "Be ready to explain what a bigram adds over unigrams alone with a concrete example.",
            "follow_up_q": "Why not use word embeddings instead of TF-IDF?",
            "follow_up_a": "Embeddings could capture semantic similarity TF-IDF misses (e.g. 'cafe' and 'restaurant' being related even without shared words), but they'd add a real dependency and complexity that a dataset this small and this vocabulary-distinct doesn't clearly need — TF-IDF was the simpler tool that was actually sufficient here.",
        },
        {
            "q": "How did you split your data for training and evaluation?",
            "short": "A stratified 75/25 train_test_split with a fixed random_state, so class proportions are preserved and results are reproducible.",
            "detailed": "train_test_split(test_size=0.25, stratify=labels, random_state=42) in app/classification/ml_classifier.py. Stratifying ensures each of the 12 categories is represented in the same proportion in both the train and test sets — with only 20 examples per category, an unstratified random split could easily leave some category with very few or zero test examples, which would make its precision/recall meaningless.",
            "verbal": "Mention stratification specifically — it's the detail that shows you thought about the small-dataset problem, not just called train_test_split with defaults.",
            "follow_up_q": "What would you do differently with a much larger dataset?",
            "follow_up_a": "I'd move to a proper train/validation/test three-way split (or cross-validation) so I'm not tuning any choices — like the ngram range or regularization strength — against the same set I report final numbers on.",
        },
        {
            "q": "Is there a risk of overfitting with your classifier, and how would you detect it?",
            "short": "Yes — I'd detect it by the test accuracy being much lower than training accuracy; here they're both very high because the dataset is small and templated.",
            "detailed": "Overfitting shows up as a gap between training performance and held-out test performance. I only report test-set metrics precisely because training accuracy on a dataset this small would be close to 100% and would tell you nothing. The bigger risk here isn't classic overfitting within this dataset — it's that the whole dataset is a narrow, templated slice of what real receipts look like, so even a model that generalizes perfectly within this data distribution may not generalize to real-world text.",
            "verbal": "Distinguish overfitting-to-the-training-set from the deeper issue of the dataset itself not representing the real world — that distinction is the sophisticated part of this answer.",
            "follow_up_q": "What regularization does LogisticRegression use by default in scikit-learn?",
            "follow_up_a": "L2 (ridge) regularization by default, controlled by the C parameter — I used scikit-learn's default C rather than tuning it, since with a dataset this small and this easy to separate, tuning regularization strength wasn't the bottleneck.",
        },
        {
            "q": "How would you retrain the classifier on real usage data instead of synthetic data?",
            "short": "Query confirmed transactions with their raw_text and category from SQLite, and feed that into the same train_and_evaluate() function instead of the synthetic CSV.",
            "detailed": "Every confirmed transaction already has raw_text (the full OCR output) and category (possibly user-edited) stored in the database. scripts/train_classifier.py currently reads data/training_data.csv, but the same train_and_evaluate() function in app/classification/ml_classifier.py would work unchanged against a query like SELECT raw_text, category FROM transactions WHERE confirmation_status='confirmed' — the training code doesn't care where the (text, label) pairs came from.",
            "verbal": "This shows you designed the code to make this extension straightforward, not just that you know it's possible in the abstract.",
            "follow_up_q": "How much real confirmed data would you want before doing this?",
            "follow_up_a": "Enough to cover most of the 12 categories with a reasonable number of examples each — with too little, or categories missing entirely, stratified train/test splitting wouldn't even be possible.",
        },
        {
            "q": "Why didn't you use cross-validation?",
            "short": "A single stratified train/test split was sufficient to get an honest, reportable number for a dataset and use case this size; cross-validation would be the next step for tuning hyperparameters rigorously.",
            "detailed": "Cross-validation is most valuable when you're comparing multiple model configurations and want a more stable estimate of which one is actually better, or when your dataset is small enough that a single split's results might be noisy. I did compare two models (logistic regression vs. Naive Bayes) informally during development, but for the final reported metrics, a single stratified split was enough to demonstrate the pipeline honestly without adding process complexity that wasn't needed for a proof-of-concept.",
            "verbal": "Fine to admit this as a scope decision, not a gap you're unaware of.",
            "follow_up_q": "When would you add k-fold cross-validation to this project?",
            "follow_up_a": "If I were tuning hyperparameters (like TF-IDF's ngram range, min_df, or logistic regression's C) systematically, or if the dataset were small enough that a single split's test accuracy might vary a lot depending on which examples happened to land in the test set.",
        },
    ],
    "NLP & Text Processing": [
        {
            "q": "Is what you built actually NLP, or just string matching?",
            "short": "Mostly rule-based text processing — regex and keyword matching — not statistical or neural NLP, and I'd say that directly.",
            "detailed": "Field extraction and the query intent parser are both pattern matching against known vocabulary, not tokenization/parsing/embeddings or any trained language model. The one place actual machine learning touches text is the category classifier's TF-IDF features, which is a standard NLP feature-engineering technique but a fairly basic one. I'm precise about this distinction rather than let 'NLP' sound more sophisticated than what's actually implemented.",
            "verbal": "This kind of honesty is exactly what this project's brief asked for, and it tends to land well in an interview — reviewers notice when a candidate doesn't oversell.",
            "follow_up_q": "What would 'real' NLP add here that regex doesn't give you?",
            "follow_up_a": "Robustness to phrasing you didn't anticipate — an embedding-based intent classifier could handle a novel way of asking 'how much did I spend' that doesn't match any of my regex patterns, at the cost of needing training data and being harder to debug when it's wrong.",
        },
        {
            "q": "How does your natural-language query parser handle Hindi/Hinglish?",
            "short": "A small hardcoded keyword table (CATEGORY_KEYWORDS) mapping Hindi/Hinglish words like 'khana' or 'pichle mahine' to the same structured intent as their English equivalents.",
            "detailed": "app/llm/query_parser.py's CATEGORY_KEYWORDS dict includes both English and transliterated Hindi words mapping to the same category ('food' and 'khana' both map to 'Food'), and separate regex pattern lists for date-range phrases include both ('last month' and 'pichl?e\\s*mahine'). This is genuinely just a lookup table extension, not any kind of language detection or translation — it works because Hinglish speakers commonly mix English and transliterated Hindi words in the same sentence, which is exactly the pattern the product brief's example questions showed.",
            "verbal": "Be precise that this is vocabulary coverage, not language understanding — don't overclaim multilingual NLP capability.",
            "follow_up_q": "Would this handle a question written entirely in Devanagari script?",
            "follow_up_a": "No — the regex patterns match transliterated (Roman-script) Hindi words, not Devanagari text; that would need separate script-aware matching or transliteration first.",
        },
        {
            "q": "Why regex instead of a proper tokenizer/parser for field extraction?",
            "short": "Receipt labels are a small, fairly standardized vocabulary — regex is sufficient, transparent, and needs no training data or extra dependency.",
            "detailed": "A general NLP pipeline (tokenization, POS tagging, named entity recognition) is built for open-ended text where you don't know the vocabulary in advance. Receipt labels are the opposite — 'Total,' 'GST,' 'Invoice No' are a bounded, known set of phrasings. Regex matching against that known set is simpler to write, simpler to debug (a wrong match traces directly to which pattern fired), and doesn't need a trained model or extra dependency.",
            "verbal": "Tie this back to 'match the tool to the actual problem' as a recurring theme across the whole project.",
            "follow_up_q": "What would break this approach?",
            "follow_up_a": "A label phrasing genuinely outside my TOTAL_LABELS/TAX_LABELS/etc. lists — regex only recognizes what it's been explicitly given, which is exactly the tradeoff I document in docs/OCR_PIPELINE.md and docs/LIMITATIONS.md.",
        },
        {
            "q": "What text cleaning do you do before extraction, and why keep it minimal?",
            "short": "Normalize line endings, collapse repeated whitespace/blank lines, strip empty lines — deliberately not aggressive, to avoid silently altering a digit or symbol.",
            "detailed": "app/extraction/cleaning.py's clean_ocr_text() only touches whitespace and blank-line structure. I explicitly avoided anything like auto-correcting spelling or 'fixing' garbled characters, because in a financial document, an automated correction that guesses wrong about a digit or currency symbol is worse than leaving noisy text for the extraction regexes to work around (or fail cleanly on).",
            "verbal": "This is a good example of 'do less, deliberately' as an engineering decision worth explaining, not just describing the code.",
            "follow_up_q": "Could aggressive cleaning ever help here?",
            "follow_up_a": "For non-financial fields like the merchant name, maybe — but I chose to apply the same conservative cleaning uniformly rather than have different rules for different fields, to keep the pipeline simple and predictable.",
        },
        {
            "q": "How would you extend the query parser to handle a question outside its current keyword vocabulary?",
            "short": "Add new keyword patterns to the existing lookup tables, or — for real open-endedness — swap the rule-based parser for an LLM-based one behind the same QueryIntent interface.",
            "detailed": "The cheap extension is adding more entries to CATEGORY_KEYWORDS, MONTH_NAMES, or the metric/date-range pattern lists — that's how I'd handle a specific new phrasing I noticed users trying. For genuinely open-ended question understanding, I'd keep the QueryIntent dataclass as the contract and swap parse_query()'s implementation to call an LLM that outputs a QueryIntent-shaped JSON object — the rest of query_engine.py (which enforces that the LLM never computes the actual number) wouldn't need to change at all.",
            "verbal": "This shows the interface was designed with this exact extension in mind — a good sign of forward-thinking architecture.",
            "follow_up_q": "Would an LLM-based parser risk reintroducing hallucination?",
            "follow_up_a": "Not for the numeric answer, because query_engine.py's structure means the LLM would only ever be choosing which existing analytics function to call and with what parameters — it still wouldn't be the LLM doing arithmetic, just interpreting intent.",
        },
        {
            "q": "What's the risk of keyword-based category detection matching the wrong thing in a query?",
            "short": "A false positive — e.g. a question that happens to contain a category keyword incidentally, not as its actual subject.",
            "detailed": "_match_category() in query_parser.py does a straightforward regex search for any known category keyword anywhere in the question. A contrived question like 'Did I spend more on travel than on food?' would only match whichever keyword the regex list checks first (in dict iteration order), not correctly detect that the question is actually comparing two categories — that comparison intent isn't in my QueryIntent schema at all.",
            "verbal": "Good to have a concrete example of the limitation ready rather than just gesturing at 'it could go wrong.'",
            "follow_up_q": "How would you support a comparison question like that?",
            "follow_up_a": "Add a new metric type ('category_comparison') to QueryIntent with two category fields, detect a comparison pattern ('more than', 'vs') in the parser, and add a corresponding branch in query_engine.py's _execute_intent().",
        },
        {
            "q": "Does merchant normalization count as NLP?",
            "short": "It's text normalization — regex suffix stripping, a lookup table, and fuzzy string matching — not NLP in the language-understanding sense.",
            "detailed": "normalize_merchant() in app/normalization/merchant.py doesn't parse meaning; it's pattern-based cleanup (strip 'Pvt Ltd'/'Internet'/'.com' style suffixes), a small alias dictionary, and rapidfuzz's token_sort_ratio for approximate string matching against known merchants. It's a standard data-cleaning technique used in a lot of real-world pipelines (deduplicating company names, addresses, etc.), and calling it 'NLP' would be overselling a fairly mechanical process.",
            "verbal": "Consistent with the project's overall stance: name things accurately rather than reaching for the more impressive-sounding term.",
            "follow_up_q": "What's token_sort_ratio actually doing?",
            "follow_up_a": "It tokenizes both strings, sorts each string's tokens alphabetically, then computes a similarity ratio between the sorted-token strings — which makes it robust to word order differences (e.g. 'Foods Swiggy' vs 'Swiggy Foods' would still score highly similar).",
        },
    ],
    "Database & Backend": [
        {
            "q": "Why no ORM (like SQLAlchemy)?",
            "short": "One table, hand-written SQL is simple enough to read directly, and an ORM would add an abstraction layer this project's scale doesn't need.",
            "detailed": "An ORM earns its keep when you have many related tables, complex joins, or want database-agnostic code. This project has one table. app/database/repository.py's raw SQL is short enough to read top to bottom and know exactly what query runs — an ORM would add a layer of translation between what I write and what SQL actually executes, which is friction without benefit at this scale.",
            "verbal": "Frame as 'right tool for the size of the problem,' the same theme as the no-REST-API and no-microservices decisions.",
            "follow_up_q": "At what point would an ORM become worth it?",
            "follow_up_a": "Multiple related tables with real foreign-key relationships and joins, or needing to support both SQLite and Postgres with the same code — neither applies here yet.",
        },
        {
            "q": "How do you prevent SQL injection?",
            "short": "Every query uses parameterized placeholders (?) — user input is never string-formatted directly into SQL.",
            "detailed": "Every conn.execute() call in app/database/repository.py passes values as a separate params tuple/list alongside a query string containing ? placeholders — sqlite3 handles safe substitution. list_transactions() builds its WHERE clause dynamically based on which filters are provided, but even there, only the column/operator structure is built as a string; the actual filter values always go through the parameterized params list, never interpolated into the SQL string itself.",
            "verbal": "Be ready to point to the exact pattern (? placeholders + params tuple) rather than give a generic 'I use parameterized queries' answer.",
            "follow_up_q": "Is there anywhere in the codebase that's more at risk, like dynamic column names?",
            "follow_up_a": "update_transaction() builds a dynamic SET clause from column names, but those names come from a hardcoded 'allowed' set intersected with the caller's dict keys — never from raw user input — so a column name can't be injected even though the SQL string itself is built dynamically.",
        },
        {
            "q": "Why is there only one index on the transactions table?",
            "short": "(confirmation_status, transaction_date) — because nearly every real query filters by status and sorts/filters by date; I didn't add indexes speculatively.",
            "detailed": "Every analytics and UI query starts from confirmation_status='confirmed' and often also filters or sorts by transaction_date, so that composite index directly serves the actual query pattern. I didn't index every column 'just in case' — extra indexes have a real cost (slower writes, more storage) that isn't worth paying without a query pattern that needs it, and at this project's data volume (dozens to low hundreds of rows) it barely matters either way, but the reasoning is the same one I'd apply at real scale.",
            "verbal": "Show you understand indexes have a cost, not just a benefit — that balance is the actual point of this question.",
            "follow_up_q": "Would you add an index on merchant or category individually?",
            "follow_up_a": "Only if I saw a specific slow query filtering heavily on just that column at real scale — I'd want to measure before adding, not guess.",
        },
        {
            "q": "How would you migrate this schema if you needed to add a new column?",
            "short": "Manually, with an ALTER TABLE — there's no migration framework (like Alembic) wired up, which is a real gap for anything beyond a single-developer prototype.",
            "detailed": "SCHEMA in app/database/db.py is a single CREATE TABLE IF NOT EXISTS statement, so it only handles the initial schema, not evolving it. Adding a column today would mean either deleting data/app.db and re-running init_db() (losing existing data — fine for this project's demo data, not fine for anything real) or manually running an ALTER TABLE against the existing file. A real migration tool would version schema changes and apply them incrementally without data loss — that's a genuine limitation I'd flag directly.",
            "verbal": "Don't dodge this one — naming the gap directly and knowing what tool would fix it (Alembic) is a better answer than pretending the current setup is fine for schema evolution.",
            "follow_up_q": "Why didn't you add a migration tool from the start?",
            "follow_up_a": "The schema didn't change enough during development to justify the overhead — but I'd add Alembic (or write manual migration scripts) before this went anywhere beyond a single-developer local project.",
        },
        {
            "q": "Explain the transaction schema's confirmation_status field and why it's a string, not a boolean.",
            "short": "It's a string ('pending'/'confirmed') rather than a boolean specifically so it can grow into more states later (e.g. 'rejected', 'needs_review') without a schema change.",
            "detailed": "A boolean confirmed/not-confirmed would work for exactly the two states this project currently has, but a string with defined valid values (tracked in app/models/transaction.py as PENDING/CONFIRMED constants) leaves room to add a third state later — like a 'rejected' status for a receipt the user decides was a duplicate or a mistake, rather than either confirming it or leaving it stuck as pending forever.",
            "verbal": "A small but real example of choosing a slightly more flexible representation for a good, specific reason — not flexibility for its own sake.",
            "follow_up_q": "Doesn't a string field risk typos/invalid values a boolean wouldn't?",
            "follow_up_a": "Yes — that's a real tradeoff. SQLite doesn't enforce an enum constraint the way Postgres could (a CHECK constraint or a native enum type), so an invalid status string could technically be written; the constants in transaction.py are a convention, not a database-level guarantee.",
        },
        {
            "q": "What happens to the raw_text field over time — doesn't it bloat the database?",
            "short": "Yes, potentially — it's a real tradeoff between keeping the full OCR output for debugging/retraining versus storage growth; I chose to keep it.",
            "detailed": "Every transaction stores its full OCR raw_text, which could be a few hundred to a couple thousand characters. For a personal expense tracker's scale (dozens to hundreds of transactions), this isn't a practical problem. I chose to keep it because it directly enables two things: the 'show raw OCR text' debug view on the verification screen, and re-training the classifier on real confirmed data later (see ML & Classification section) — both need the original text, not just the extracted fields.",
            "verbal": "Good example of naming a real tradeoff and explaining why you accepted it rather than pretending there's no downside.",
            "follow_up_q": "How would you handle this at real scale, with millions of transactions?",
            "follow_up_a": "I'd consider moving raw_text to separate cold storage (like S3) referenced by a key, rather than inline in the primary transactional table, so the hot table used for dashboard queries stays small and fast.",
        },
        {
            "q": "Why does list_transactions() return plain dicts instead of Transaction dataclass instances?",
            "short": "Because it reads from the database (where every column, including nullable ones, is already resolved), and the callers (Streamlit, pandas) work more naturally with dicts than with a strict dataclass.",
            "detailed": "insert_transaction() takes a Transaction dataclass as input, because that's a natural, type-checked shape to build in Python before writing. But list_transactions()/get_transaction() return sqlite3.Row objects converted to plain dicts, because the direct consumers — pandas.DataFrame(transactions) in app/analytics/metrics.py, and Streamlit's data_editor — work naturally with dicts/records, and re-wrapping every row back into a dataclass just to immediately convert it again would be unnecessary ceremony.",
            "verbal": "This is a small but real 'why isn't this symmetric' question worth having a specific answer for rather than looking surprised by the inconsistency.",
            "follow_up_q": "Doesn't that inconsistency (dataclass in, dict out) risk confusion?",
            "follow_up_a": "A little — the tradeoff was optimizing for what each direction's actual consumers need rather than forcing a single representation everywhere; a stricter design might wrap read results in a dataclass too, at the cost of dataclass-to-dict conversions in analytics.",
        },
        {
            "q": "How do you handle concurrent writes to the database?",
            "short": "SQLite's own file-level locking handles it correctly for this app's usage — a single Streamlit session, one write at a time — but it isn't built for real concurrent multi-user writes.",
            "detailed": "Each repository function opens its own short-lived connection, does its work, and closes it (get_connection() -> ... -> conn.close()), rather than holding one long-lived connection — this minimizes how long any lock is held. SQLite handles a single writer correctly by default (it'll block/retry rather than corrupt data), which is sufficient for one person using the Streamlit app. It is not designed for many concurrent writers, which is exactly why docs/LIMITATIONS.md names this directly as a reason this wouldn't scale to multiple simultaneous users without moving to Postgres.",
            "verbal": "Name the actual mechanism (short-lived connections, SQLite's built-in locking) rather than a vague 'SQLite handles it.'",
            "follow_up_q": "What would you change first to support multiple concurrent users?",
            "follow_up_a": "Move to Postgres, which handles concurrent writers properly with MVCC, and add a connection pool instead of per-call connections.",
        },
    ],
    "LLM": [
        {
            "q": "How do you prevent the LLM from hallucinating a wrong number?",
            "short": "By never asking it to produce one — the LLM only rewords an answer that's already been computed by app/analytics, and the prompt explicitly instructs it not to alter any numbers.",
            "detailed": "In app/llm/query_engine.py, result_value and template_answer are both fully computed before provider.is_available is even checked. If an LLM is used, _reword_with_llm()'s prompt hands over the template answer as a stated fact ('Factual answer (do not alter the numbers)') and asks only for a rephrasing. There's no code path where the model's output becomes the source of a number shown to the user — the architecture makes hallucinated numbers structurally impossible, not just unlikely.",
            "verbal": "This is probably the single most important question in the whole interview guide — answer it precisely, pointing at the actual code structure, not just the policy.",
            "follow_up_q": "Could the LLM still change a number by rewording carelessly, even with that instruction?",
            "follow_up_a": "In principle yes, if it ignored the instruction — that's a real limitation of prompt-level constraints. A stricter version would validate that every digit in the LLM's output matches the template answer's digits before displaying it, and fall back to the template if they don't match; that validation isn't currently implemented.",
        },
        {
            "q": "Why is the intent parser rule-based instead of using the LLM to parse the question?",
            "short": "So natural-language querying works with zero API keys, and because the actual question vocabulary this app needs to understand is small and known in advance.",
            "detailed": "The product requirement was that core features — including natural-language querying — work without any LLM configured. A rule-based parser (keyword/regex matching, including Hindi/Hinglish keywords) is what makes that possible specifically for this feature. It's also a better-fit tool: the example questions in the brief are all answerable from a bounded set of metric words, category words, and date-range phrases, which is exactly what a keyword parser is good at.",
            "verbal": "Connect this back to the 'LLM is optional, never load-bearing' principle that runs through the whole project.",
            "follow_up_q": "What's the ceiling of this rule-based approach?",
            "follow_up_a": "It only understands the keywords it's been given — a genuinely novel phrasing outside that vocabulary falls back to default assumptions (total spending, this month) rather than failing, which is safer than guessing wrong but does mean there's a real limit to what it understands.",
        },
        {
            "q": "What happens if the OpenAI API call fails or times out?",
            "short": "answer_query() catches the exception and falls back to the template answer — the user still gets a correct answer, just not LLM-reworded.",
            "detailed": "The call to provider.complete() inside answer_query() is wrapped in a try/except that catches any exception (bad key, network failure, rate limit, timeout) and sets final_answer back to template_answer, with used_llm=False. This was a deliberate design choice: an LLM failure should degrade the feature gracefully to 'plain sentence' rather than break the query feature or show the user an error for something that isn't really broken — the correct answer was already computed regardless.",
            "verbal": "Point to the try/except specifically — this is a concrete, checkable claim about the code, not just a design philosophy.",
            "follow_up_q": "Is that except clause too broad (catching all exceptions)?",
            "follow_up_a": "It's intentionally broad because the point is 'anything that could go wrong with an external network call should degrade gracefully' — I'd only narrow it if I found a specific exception type I wanted to handle differently, which hasn't come up.",
        },
        {
            "q": "Why didn't you test the OpenAI provider against a real API key?",
            "short": "No API key was available during development — I say that directly in the docs rather than imply it was tested when it wasn't.",
            "detailed": "OpenAIProvider is implemented against OpenAI's documented chat completions API shape, and its is_available/complete() interface is exercised by tests up to the provider boundary (tests/test_query_engine.py tests the NullProvider path fully, since that's what's actually verifiable without a key). The live API call itself has not been run. docs/LLM_ARCHITECTURE.md and docs/LIMITATIONS.md both say this explicitly, because claiming something was tested when it wasn't is exactly the kind of dishonesty this project's brief said to avoid.",
            "verbal": "Don't soften this — state it plainly. Interviewers generally respect this kind of precision about what was and wasn't verified far more than a vague 'yes it works.'",
            "follow_up_q": "How confident are you it would work if you added a key?",
            "follow_up_a": "Reasonably confident, since it's a small, standard implementation against a well-documented API — but 'reasonably confident' and 'verified' are different things, and I'd want to actually run it before claiming it works.",
        },
        {
            "q": "How would you add a second LLM provider, like Anthropic's Claude?",
            "short": "Write one new file implementing the LLMProvider protocol (complete(), is_available), and add one branch to the factory.",
            "detailed": "app/llm/base.py defines LLMProvider as a small Protocol — anything with complete(prompt) -> str and an is_available property satisfies it structurally, no inheritance required. A new AnthropicProvider class implementing those two things, plus one new elif branch in app/llm/factory.py's get_default_provider() checking for an Anthropic-specific environment variable, is the entire change — nothing in query_engine.py or anywhere else would need to know a new provider exists.",
            "verbal": "This is a good example to have ready of the provider interface actually paying off, not just existing for its own sake.",
            "follow_up_q": "Why a Protocol instead of an abstract base class?",
            "follow_up_a": "A Protocol is structural typing — NullProvider and OpenAIProvider don't need to inherit from a common base class, they just need to have the right methods, which keeps each provider file simpler and avoids an unnecessary inheritance hierarchy for what's really just 'two functions.'",
        },
        {
            "q": "Does the LLM ever see raw transaction data?",
            "short": "No — it only ever receives the user's question text and one already-computed answer sentence, never the underlying transaction rows.",
            "detailed": "_reword_with_llm()'s prompt contains exactly two pieces of data: the user's original question and the template_answer sentence. It never receives the transaction list, merchant names, or any other raw data from the database. This matters for privacy as much as for hallucination prevention — README's Privacy Considerations section states this directly.",
            "verbal": "Connects the technical answer to the privacy angle — worth mentioning both if there's time.",
            "follow_up_q": "Could this design still leak sensitive information indirectly?",
            "follow_up_a": "The template answer itself could contain a specific merchant name or amount (e.g. 'You spent the most on X'), so if the API provider logs requests, that specific fact would be visible to them — which is a real, if narrow, privacy consideration for anyone using this with a live API key on genuinely sensitive data.",
        },
        {
            "q": "What's the difference between how you use the LLM here versus a typical 'chat with your data' AI product?",
            "short": "A typical chatbot-over-data product often lets the LLM see raw data and generate both the answer and its phrasing; here the LLM never sees raw data and never generates the answer itself, only the phrasing of an answer already computed elsewhere.",
            "detailed": "Many RAG-style 'ask your data' products feed retrieved rows or documents into the LLM's context and let it synthesize an answer directly — which is flexible but creates a real hallucination risk when the answer involves arithmetic the model has to do itself (LLMs are not reliable calculators). This project inverts that: deterministic code does 100% of the calculation, and the LLM's only job, when used at all, is making the already-correct sentence sound more natural. That's a narrower, safer use of the LLM specifically because the domain (financial figures) has zero tolerance for a wrong number.",
            "verbal": "This is a good chance to show you understand the broader industry pattern (RAG/chat-over-data) and can articulate specifically why you didn't use it here.",
            "follow_up_q": "Are there parts of this app where a RAG-style approach WOULD make sense?",
            "follow_up_a": "Possibly for something like 'summarize my spending habits in a paragraph' where there's no single correct number at stake — a more generative, LLM-driven answer would be reasonable there in a way it isn't for 'how much did I spend on Food.'",
        },
        {
            "q": "How do you keep API costs/usage bounded if this had real LLM traffic?",
            "short": "The prompt is short (one question + one short answer, max_tokens=200), and the LLM is only ever called once per typed query, not per dashboard render.",
            "detailed": "OpenAIProvider.complete() sets max_tokens=200 and temperature=0.2 — a short, deterministic-leaning completion, appropriate for 'reword this one sentence,' not an open-ended generation task. The LLM is only invoked from the Ask a Question page, on an explicit user action, never automatically on every dashboard load — the dashboard and analytics pages never touch the LLM layer at all, keeping any real-world API cost proportional to how often someone actually asks a typed question.",
            "verbal": "Good to have the specific numbers (max_tokens=200, temperature=0.2) ready rather than a vague 'it's efficient.'",
            "follow_up_q": "Why temperature=0.2 instead of 0?",
            "follow_up_a": "A low but non-zero temperature still allows some natural variation in phrasing (which is the whole point of rewording), while keeping the output close to deterministic — temperature=0 would be more rigid than necessary for a task that's explicitly about natural-sounding language, not precision.",
        },
    ],
    "System Design": [
        {
            "q": "How would you design this if it needed to support multiple users?",
            "short": "Add a user_id column and auth, move to Postgres, and scope every query in repository.py by user_id — the interface shape wouldn't need to change much.",
            "detailed": "The repository functions already take explicit parameters rather than reaching into global state, so adding a required user_id parameter to each of them (and a WHERE user_id = ? clause) is a relatively contained change. The bigger additions would be an actual authentication system (Streamlit itself doesn't have built-in multi-user auth — you'd need something like streamlit-authenticator or move the UI to a framework with session-based auth), and switching from SQLite to Postgres for real concurrent-writer support.",
            "verbal": "Have the concrete plan ready (user_id column, Postgres, auth library) rather than a vague 'add authentication.'",
            "follow_up_q": "Would you keep Streamlit for a multi-user product?",
            "follow_up_a": "For a real multi-user product I'd lean toward a proper frontend (React or similar) talking to a REST/GraphQL API — Streamlit's per-session single-process model isn't really built for that, which is part of why I scoped this project as single-user to begin with.",
        },
        {
            "q": "Where would OCR processing become a bottleneck, and how would you fix it?",
            "short": "Synchronous OCR on the upload request blocks the UI for a few seconds; at any real scale I'd move it to a background job queue.",
            "detailed": "Currently, process_upload() runs OCR synchronously inside the Streamlit request — fine for one user uploading occasionally, but it means the UI is blocked for however long Tesseract takes on that file. At scale, I'd push the OCR + extraction work onto a background task queue (Celery with Redis, or a cloud queue), return immediately with a 'processing' state, and update the transaction to 'pending verification' once the background job completes — the frontend would poll or use websockets to reflect the update.",
            "verbal": "Name a specific real tool (Celery/Redis) rather than a vague 'use a queue' — it signals you've actually thought through the mechanics.",
            "follow_up_q": "How would the user experience change with async processing?",
            "follow_up_a": "The upload would show a 'processing' status immediately instead of blocking, and the user would see the extracted fields appear on the verification screen once the background job finishes — a small UX change, but a necessary one for the backend change to make sense.",
        },
        {
            "q": "How would you cache dashboard analytics if recomputing them got slow?",
            "short": "Cache aggregate results keyed by a hash of the relevant filter (e.g. month), invalidated whenever a transaction in that period is inserted/edited/deleted.",
            "detailed": "Right now every dashboard load recomputes pandas aggregations from a fresh database query — fine at this project's data volume, but at real scale you'd want to avoid recomputing, say, all-time totals on every page view. A reasonable approach: cache monthly aggregates (Streamlit has @st.cache_data, or you'd use Redis for a non-Streamlit app) keyed by month, and invalidate the specific month's cache entry when a write touches a transaction dated in that month, rather than invalidating everything on every write.",
            "verbal": "Be ready to explain cache invalidation specifically — that's usually the harder part of this question, not caching itself.",
            "follow_up_q": "What's the risk of caching here?",
            "follow_up_a": "Showing stale numbers if invalidation is missed on some write path — which is exactly the kind of subtle bug that makes me cautious about adding caching before it's actually needed; premature caching is a common source of hard-to-find staleness bugs.",
        },
        {
            "q": "What single point of failure exists in this system, and how would you address it?",
            "short": "The single SQLite file — if it's corrupted or the disk fails, all data is gone; I'd add regular backups and, at scale, move to a replicated database.",
            "detailed": "data/app.db is a single file with no replication, no automated backups, and no encryption at rest — all explicitly named in docs/LIMITATIONS.md. For a real deployment, the immediate fix is scheduled backups (even a simple cron job copying the file to cloud storage); the more thorough fix is moving to a managed database service (like RDS/Cloud SQL Postgres) that handles replication and point-in-time recovery for you.",
            "verbal": "Name the actual current gap specifically (no backups, no replication) rather than answer this generically.",
            "follow_up_q": "Would you add encryption at rest for a real deployment?",
            "follow_up_a": "Yes — financial data warrants it, and a managed Postgres service typically offers this as a configuration option rather than something I'd have to build myself.",
        },
        {
            "q": "How would you version the API/data contract if the UI and backend were split into separate services?",
            "short": "I'd version the QueryIntent/Transaction/analytics response shapes explicitly and treat app/ as the contract, adding fields rather than changing existing ones' meaning.",
            "detailed": "Right now app/models/transaction.py's Transaction dataclass and app/llm/query_parser.py's QueryIntent dataclass are the de facto contracts between layers, even in-process. If split into services, I'd formalize those as an API schema (e.g. Pydantic models exposed via FastAPI, matching the project's sibling Resume Analyzer project's stack choice), version the API path (/v1/...), and follow additive-change discipline — add new optional fields rather than repurpose or remove existing ones, to avoid breaking whichever service hasn't been updated yet.",
            "verbal": "Fine to reference general API versioning practice here since this project doesn't have a real API layer to point to directly — just be clear you're speaking in general terms for this specific question.",
            "follow_up_q": "Why didn't you build this as a versioned API from the start?",
            "follow_up_a": "Because there was only ever one client (the Streamlit UI, in the same process) — versioning is a discipline that matters once multiple independent consumers exist, which wasn't this project's actual situation.",
        },
        {
            "q": "How would you monitor this system in production?",
            "short": "Application logs for OCR/extraction failures and LLM fallback rate, plus basic uptime monitoring — none of which is built currently, since there's no production deployment.",
            "detailed": "I'd want to log (not just silently handle) every OcrResult(success=False) and every LLM fallback-due-to-exception event, so I could see failure rates over time rather than only what an individual user sees in the UI. For a real deployment, I'd add structured logging (not print statements) and a basic uptime/error-rate dashboard — none of this exists in the current codebase, which is appropriate for a local single-user prototype but would be a first priority before any real production use.",
            "verbal": "Honest that this doesn't exist yet, paired with a concrete plan for what you'd add first.",
            "follow_up_q": "What's the single most important metric you'd want visibility into?",
            "follow_up_a": "The OCR/extraction failure rate — it's the leading indicator of whether the core pipeline is actually working for real users' receipts, versus everything downstream (categorization, analytics) which depends on extraction having worked in the first place.",
        },
    ],
    "Testing": [
        {
            "q": "What's your testing strategy for this project?",
            "short": "53 pytest unit tests, one module per app/ package, focused on hand-calculated expected values and explicit edge cases rather than just 'does it run.'",
            "detailed": "Each app/ package (extraction, normalization, classification, analytics, database, the query parser/engine, validators) has a corresponding tests/test_*.py module. Most tests construct a small, specific input, work out the correct output by hand, and assert exact equality — e.g. test_analytics.py's SAMPLE_TRANSACTIONS with hand-verified category totals. Edge cases (empty data, zero-division in percentage_change, corrupted/oversized uploads, unparseable dates) are tested explicitly, not left to chance.",
            "verbal": "Mention the specific number (53) and the hand-calculated-value pattern — it's more convincing than a vague description of 'good test coverage.'",
            "follow_up_q": "What's NOT covered by these tests?",
            "follow_up_a": "The Streamlit UI layer itself isn't tested (no UI/integration tests) — I verified the UI manually and with Playwright screenshots showing real, working output, but there's no automated test asserting the upload page's behavior the way there is for the underlying functions it calls.",
        },
        {
            "q": "Why do your database tests use a temporary file instead of an in-memory SQLite database?",
            "short": "A temp file more closely matches real usage (connections opened/closed per call, like the actual repository functions do) and avoids any risk of touching the real data/app.db.",
            "detailed": "tests/test_repository.py's temp_db fixture creates a real temporary file via tempfile.mkstemp(), sets DATABASE_PATH to point at it via monkeypatch, and removes it after the test. An in-memory database (':memory:') is faster, but each new sqlite3.connect(':memory:') call gets its own separate empty database — since repository.py's functions each open a fresh connection per call (rather than reusing one long-lived connection), an in-memory approach would mean every function call gets a blank database, which wouldn't work at all for this code's connection pattern.",
            "verbal": "This is a good 'why' to have ready — it's not an arbitrary choice, it's forced by how the connection-per-call pattern actually works.",
            "follow_up_q": "Would you refactor the connection pattern to support in-memory testing?",
            "follow_up_a": "I'd only do that if test speed became a real problem — 53 tests including the file-based database ones run in well under a second, so there's no actual performance issue motivating the change.",
        },
        {
            "q": "How do you test code that depends on 'today's date,' like the insights generator?",
            "short": "generate_insights() and answer_query() both accept an explicit as_of parameter, defaulting to date.today() but overridable in tests.",
            "detailed": "If these functions always used date.today() internally, tests would be flaky — passing today, failing tomorrow, or requiring awkward workarounds. Instead, generate_insights(df, as_of=None) and answer_query(question, transactions, as_of=None) both accept an explicit as_of date, defaulting to today() only when not provided. Tests pass a fixed date (e.g. date(2026, 8, 20)) so results are fully deterministic and reproducible regardless of when the test suite actually runs.",
            "verbal": "This is a specific, checkable design pattern (dependency injection of the 'current time') worth citing by name if asked about testability more generally.",
            "follow_up_q": "Is this pattern used consistently everywhere the app needs 'now'?",
            "follow_up_a": "Yes for the two places that compute date-relative analytics (insights and query answers); the database layer's created_at/updated_at timestamps use datetime.now() directly since those genuinely should reflect real wall-clock time when a row was written, not a testable input.",
        },
        {
            "q": "Give an example of a test that caught a real bug during development.",
            "short": "test_strips_internet_pvt_ltd_suffix in test_normalization.py failed initially because standalone 'Pvt.' wasn't in the corporate-suffix list, only the combined 'Pvt Ltd' pattern was.",
            "detailed": "I wrote a test expecting normalize_merchant('Swiggy Internet Pvt.') == 'Swiggy', ran the suite, and it failed — the actual output was 'Swiggy Pvt' because my CORPORATE_SUFFIXES list only had a combined r'pvt\\.?\\s*ltd\\.?' pattern requiring both words together, and 'Internet' got stripped by a separate rule leaving 'Pvt.' standalone with nothing left to remove it. I added a standalone r'pvt\\.?\\b' pattern and the test passed. This is a real example, not a hypothetical — it's in this project's actual commit history.",
            "verbal": "Have this specific example ready — a concrete bug-and-fix story is much more convincing than an abstract claim that tests 'help catch bugs.'",
            "follow_up_q": "How did you decide what test cases to write in the first place?",
            "follow_up_a": "By thinking through actual variations I'd expect to see in real receipts (uppercase names, corporate suffixes, near-duplicate spellings) rather than only testing the happy path with one clean example.",
        },
        {
            "q": "How would you test the OCR pipeline itself, given it depends on an external binary (Tesseract)?",
            "short": "I test extraction logic separately from OCR (with hand-written text fixtures, no Tesseract needed), and verified the full pipeline manually against synthetic generated receipt images.",
            "detailed": "tests/test_extraction.py tests extract_amount/extract_date/extract_merchant/etc. against plain Python strings — no image, no Tesseract dependency, so these tests run anywhere Python runs. For the full OCR pipeline (image -> Tesseract -> extraction -> normalization -> categorization), I don't have automated tests, but I did run it end-to-end against scripts/generate_sample_receipts.py's synthetic images and manually inspected every extracted field — that's how I found and fixed the date-parsing and largest-number-fallback bugs.",
            "verbal": "Be clear about what IS automated (extraction logic) versus what was manually verified (the full OCR pipeline) — don't blur the two.",
            "follow_up_q": "Why not automate the full-pipeline test too?",
            "follow_up_a": "It's a reasonable next step — a test that runs process_upload() against a checked-in sample_receipts/ image and asserts specific extracted values would catch regressions in the full pipeline, not just the extraction-logic layer; I verified it manually this round but would want that automated for anything beyond a portfolio project.",
        },
        {
            "q": "What testing would you add before considering this production-ready?",
            "short": "Automated full-pipeline tests against real (not just synthetic) receipts, load/concurrency testing, and integration tests for the Streamlit UI itself.",
            "detailed": "Beyond what exists: (1) automated tests running the actual OCR pipeline against a checked-in set of sample images, not just extraction logic in isolation; (2) tests against real, messy receipt photos, not only clean synthetic ones — synthetic data can't reveal every real-world failure mode; (3) some form of UI/integration testing (Streamlit apps can be tested with tools like streamlit's own AppTest or Playwright-driven end-to-end tests, similar to how I used Playwright to verify the app manually and capture screenshots); (4) load testing if this were ever going to serve multiple concurrent users.",
            "verbal": "A good, specific answer for 'what's next' questions — shows you know the current test suite's actual boundary, not just that tests exist.",
            "follow_up_q": "Which of these would you prioritize first?",
            "follow_up_a": "Real (not synthetic) receipt testing — it's the one most likely to surface extraction failures that actually matter to a real user, versus concurrency/load testing which only matters once there's real multi-user traffic to handle.",
        },
    ],
    "General / Beginner": [
        {
            "q": "What does this project do, in one sentence?",
            "short": "It turns a photo or PDF of a receipt into a categorized expense record, with a dashboard and a natural-language query feature on top.",
            "detailed": "See the 30-second pitch at the start of this guide for the fuller version — but the one-sentence answer is deliberately about the outcome (categorized expense record, dashboard, queries) rather than the technology list, because that's what actually explains what the app does for a user.",
            "verbal": "Lead with the user outcome, not the tech stack — save 'Tesseract, scikit-learn, Streamlit' for when they ask how it's built.",
            "follow_up_q": "Who would actually use this?",
            "follow_up_a": "Students, freelancers, or anyone tracking personal/small-business expenses who currently either doesn't track receipts at all or does it entirely manually.",
        },
        {
            "q": "What's the difference between OCR and machine learning in this project?",
            "short": "OCR (Tesseract) turns an image into text; machine learning (the optional classifier) is a separate, later step that predicts a category from that text.",
            "detailed": "These are genuinely different technologies solving different problems. OCR is a mature, mostly-solved (for printed text) computer vision problem — recognizing character shapes in pixels. The category classifier is a text classification problem — given already-extracted text, predict which of 12 labels it belongs to. They're sequential in the pipeline (OCR has to happen before there's any text to classify) but are implemented with completely different techniques (Tesseract's LSTM models vs. scikit-learn's TF-IDF + logistic regression).",
            "verbal": "A good one to answer clearly and simply — this question is testing basic conceptual clarity, not depth.",
            "follow_up_q": "Is OCR itself a form of machine learning?",
            "follow_up_a": "Modern Tesseract (v4+) uses an LSTM neural network internally, so yes, in that sense OCR here does rely on a trained model — but it's a pretrained, general-purpose model I use as-is, not something I trained myself, which is an important distinction from the category classifier I did train.",
        },
        {
            "q": "What is a regular expression, and why did you use them so heavily?",
            "short": "A pattern-matching language for finding structured text; I used them because receipt fields (labels, amounts, dates) follow fairly predictable text patterns.",
            "detailed": "A regex describes a pattern to search for in text — e.g. r'\\d{1,2}[/-]\\d{1,2}[/-]\\d{4}' matches a date like '26/08/2026'. Extraction, normalization, and query parsing in this project all lean on regex because receipt text and typed questions both have enough structure (known labels, known date formats, known keywords) that pattern matching is a simpler, more transparent tool than something heavier like a trained NER model.",
            "verbal": "Good to have one concrete regex example memorized (the date pattern is a clean one) to make this answer tangible rather than abstract.",
            "follow_up_q": "What's a limitation of using regex for this?",
            "follow_up_a": "It only recognizes patterns you've explicitly anticipated — a genuinely novel label phrasing or date format outside the patterns I wrote won't be recognized, which is a real, documented limitation (see docs/OCR_PIPELINE.md).",
        },
        {
            "q": "Why is a SQLite database used instead of just saving to a CSV file?",
            "short": "SQLite supports proper querying (filtering, sorting), safe concurrent-enough access for one user, and atomic updates — a CSV would require re-writing the whole file for every edit.",
            "detailed": "A CSV file has no query language — filtering transactions by date range and category would mean reading the whole file into memory and filtering in Python every time, and editing a single row means rewriting the entire file. SQLite gives indexed queries, atomic single-row updates, and a standard interface (SQL) that's easy to extend later. For a project with edit/delete/filter requirements like this one, a real database is the appropriate tool, not overkill.",
            "verbal": "Simple, direct comparison — good for showing basic database reasoning.",
            "follow_up_q": "When would a CSV actually be the right choice?",
            "follow_up_a": "For write-once, read-mostly data with no need for queries or concurrent edits — like the static training_data.csv used for classifier training, which is exactly why that one IS a CSV rather than a database table.",
        },
        {
            "q": "What is a dataclass, and why did you use one for Transaction?",
            "short": "A Python class that auto-generates __init__, __repr__, and equality methods from a list of typed fields — used here so Transaction has one clear, typed definition shared across every layer.",
            "detailed": "@dataclass on app/models/transaction.py's Transaction class means I just declare the fields (merchant: str, amount: Optional[float], etc.) and Python generates the boilerplate constructor and comparison methods. Using one shared dataclass instead of passing around loose dicts with the same keys everywhere means a typo in a field name is more likely to be caught (by type checking / IDE support) rather than silently doing nothing at runtime.",
            "verbal": "Keep this simple and concrete — a good beginner-level question to answer clearly without overcomplicating it.",
            "follow_up_q": "Why not use a plain class with __init__ written by hand instead?",
            "follow_up_a": "It would work identically, just with more boilerplate code to write and maintain for the same result — dataclasses exist specifically to remove that repetitive code for simple data-holding classes like this one.",
        },
        {
            "q": "What is a confidence score, in plain terms?",
            "short": "A number indicating how sure the system is about a value it produced — here, a heuristic 0-1 score about whether extraction likely got the right fields, not a guarantee of correctness.",
            "detailed": "In this project specifically, extraction_confidence isn't derived from any statistical model — it's a hand-written heuristic that adds up points for whether a labeled total was found, whether a date was found, and whether a merchant was found. It's useful as a signal for when to show the user an extra warning, but it's explicitly not a calibrated probability (e.g. a 0.75 doesn't mean '75% chance this is correct' in any statistically rigorous sense).",
            "verbal": "Be precise about the difference between a heuristic score and a calibrated probability — that precision is worth demonstrating even on a 'beginner' question.",
            "follow_up_q": "How would you make this a genuinely calibrated probability?",
            "follow_up_a": "You'd need labeled ground-truth data (extractions marked correct/incorrect by a human) to actually fit a model predicting P(correct) from features like OCR confidence and which extraction path was used — a meaningfully bigger undertaking than the current heuristic.",
        },
        {
            "q": "What's the difference between a merchant and a category in this app?",
            "short": "Merchant is who you paid (e.g. Swiggy); category is what kind of expense it was (e.g. Food) — one merchant usually maps to one category, but they're stored as separate fields.",
            "detailed": "They're related but distinct pieces of information, and keeping them as separate database columns (rather than, say, only storing category and losing the specific merchant) matters because the dashboard's merchant-level insights (like 'Swiggy and Zomato account for 62% of Food spending') need the merchant, not just the category.",
            "verbal": "Simple distinction, good to answer crisply without over-explaining.",
            "follow_up_q": "Could two different merchants ever have different categories on different transactions?",
            "follow_up_a": "Not automatically in the current rule-based system — MERCHANT_CATEGORY maps a merchant name to one fixed category — but a user can manually recategorize any individual transaction on the transaction table, which does allow per-transaction overrides.",
        },
        {
            "q": "Why does the app validate uploaded files before processing them?",
            "short": "To reject obviously bad input early (wrong type, empty, corrupted, oversized) rather than let OCR fail confusingly on something that was never going to work.",
            "detailed": "app/utils/validators.py checks file extension against an allowlist, rejects empty files, rejects files over 15MB, and does a real corruption check (PIL's Image.verify() for images, a %PDF magic-byte check for PDFs) — all before the file reaches OCR. This gives the user a clear, specific error message ('Unsupported file type' vs. 'File is empty' vs. 'Image file is corrupted') instead of a confusing downstream failure from Tesseract or pdf2image.",
            "verbal": "Frame this as good input-handling practice broadly, not just a receipt-specific detail.",
            "follow_up_q": "Why check file extension instead of just trying to open the file and seeing if it works?",
            "follow_up_a": "Extension checking is a fast, cheap first filter before doing any real work — but it's not sufficient alone (a file could have a .png extension and not actually be a valid PNG), which is exactly why there's also a real content-based corruption check afterward.",
        },
    ],
    "Tricky Follow-ups": [
        {
            "q": "If the rule-based categorizer and the ML classifier disagree, and the rule-based one is wrong, why not trust the ML model instead?",
            "short": "Because I don't have evidence the ML model is more often right — its measured accuracy is on synthetic data, and I have no real-world comparison between the two.",
            "detailed": "This isn't a case where I'm confident the rule-based system is always right — merchant/keyword tables can definitely miss or misclassify. The honest answer is I chose the more explainable default because I couldn't defend the ML model's real-world accuracy with the evaluation I actually have (synthetic data). If I had a labeled set of real disagreements between the two systems, with ground truth, I could make an evidence-based call about which to trust more — I don't have that yet.",
            "verbal": "This question is testing whether you'll defend a position past the evidence — the right move is to say plainly what you don't know.",
            "follow_up_q": "How would you gather that evidence?",
            "follow_up_a": "Log every case where the rule-based and ML categorizations disagree, and use the user's final confirmed category (after they review it) as ground truth — over time that builds exactly the real-world comparison dataset needed.",
        },
        {
            "q": "Your ML classifier gets 98% accuracy but you say not to trust it — isn't that contradictory?",
            "short": "No — a metric can be a real, correctly-measured number and still not generalize to a different, harder distribution of data than what it was measured on.",
            "detailed": "98% accuracy on the held-out synthetic test set is a true, reproducible fact about how the model performs on data from the same distribution as its training data. My claim isn't that the number is wrong — it's that the training/test distribution (templated synthetic text with distinct per-category vocabulary) is easier than real, messy OCR'd receipts, so the same model would likely score lower on the harder, real distribution. That's not a contradiction, it's the standard distinction between in-distribution and out-of-distribution performance.",
            "verbal": "This is exactly the kind of pushback question worth practicing out loud — staying calm and precise about the distinction matters more than the content itself.",
            "follow_up_q": "How would you actually quantify the gap between synthetic and real-world performance?",
            "follow_up_a": "Collect and label a set of real receipts, run the model against them, and directly compare that accuracy to the 98% synthetic-test number — until that's done, the size of the gap is genuinely unknown, not just assumed to be large.",
        },
        {
            "q": "Couldn't you have just used GPT-4 to extract every field directly from the receipt image, instead of building all this OCR/regex machinery?",
            "short": "You could, and it might even work better on hard cases — but it would cost money per receipt, require an API key to run the core feature at all, and be harder to debug when it's wrong.",
            "detailed": "A vision-capable LLM extracting fields directly is a real, increasingly common approach, and I wouldn't claim my regex pipeline beats it on raw accuracy for hard receipts. But it would violate this project's actual requirement that core features work with zero API keys, would add a per-receipt cost that a free/local pipeline doesn't have, and — importantly for a portfolio project — would replace something I built and can fully explain with an API call whose internal reasoning I can't inspect or defend in the same depth.",
            "verbal": "Don't be defensive about this one — acknowledge the alternative is legitimate, then give your specific, real reasons for not choosing it.",
            "follow_up_q": "Would you ever add that as an option?",
            "follow_up_a": "Yes, as an optional alternative extraction path behind the same interface OCR uses now — similar to how the LLM query layer is optional — for users who have an API key and want potentially better accuracy on hard receipts, while keeping the free/local path as the default that always works.",
        },
        {
            "q": "You said the LLM never computes numbers — but doesn't the query PARSER deciding which category/date-range to filter by also involve a kind of 'computation' that could be wrong?",
            "short": "Yes — intent parsing can genuinely misinterpret a question, and that's a real error mode distinct from, but related to, hallucination.",
            "detailed": "This is a fair distinction to draw. 'The LLM never computes the number' protects against a specific failure mode — a fabricated or arithmetically wrong figure. It does NOT protect against the parser (rule-based here, not even LLM-based) choosing the wrong intent — e.g. misreading which category or date range the user meant. If that happens, the app confidently returns a real, correctly-computed number... for the wrong question. That's a genuine limitation, and the 'How this was answered' expander on the Ask a Question page exists specifically so a user can catch that kind of misinterpretation, since the number itself will always look legitimate.",
            "verbal": "This is a genuinely sharp follow-up — acknowledging the distinction clearly, rather than conflating 'no hallucinated numbers' with 'no possible wrong answers,' is exactly the right move.",
            "follow_up_q": "How would you reduce intent-misparsing errors?",
            "follow_up_a": "Show the parsed intent to the user before executing the query (rather than only after, in a collapsed expander) so a misparse is visible immediately, or add a confirmation step for low-confidence parses — similar in spirit to the extraction confidence gate already used for receipts.",
        },
        {
            "q": "Your merchant fuzzy-matching threshold is 90 — how do you know that's the right number, versus just something that seemed reasonable?",
            "short": "I don't have rigorous evidence it's optimal — it's a manually chosen, documented tradeoff, not a tuned value, and I say that directly in docs/LIMITATIONS.md.",
            "detailed": "FUZZY_MATCH_THRESHOLD = 90 in app/normalization/merchant.py was chosen by trying it against a handful of test cases (genuine near-duplicates like 'Swigy' vs 'Swiggy' matching, clearly different merchants not matching) and picking a value that handled those correctly. That's a reasonable starting point, not a rigorously tuned parameter — properly tuning it would need a labeled dataset of merchant-name pairs marked 'should match' / 'should not match,' computing precision/recall at different thresholds, and picking the threshold that best balances false merges against missed matches.",
            "verbal": "Admitting 'I picked a value that worked on my test cases, not a tuned optimum' is a stronger answer than pretending 90 is scientifically derived.",
            "follow_up_q": "What would go wrong with a threshold that's too low, versus too high?",
            "follow_up_a": "Too low risks false merges — two genuinely different merchants (like 'Star Bazaar' and 'Star Bucks') getting collapsed into one; too high risks missing real near-duplicates that should have merged, leaving fragmented merchant data on the dashboard.",
        },
        {
            "q": "If a user uploads someone else's receipt or a fraudulent one, does your system catch that?",
            "short": "No — this system has no fraud detection at all; it trusts that whatever the user uploads and confirms is a legitimate representation of their own spending.",
            "detailed": "There's no verification that a receipt is genuine, unaltered, or belongs to the uploading user — the system extracts and stores whatever data is on the image. This is appropriate for the stated use case (a personal expense tracker where the user has every incentive to enter their own real data accurately) but would be a serious gap for any use case involving reimbursement, audit, or multi-party trust, where someone might have an incentive to submit altered or fraudulent receipts.",
            "verbal": "Good to state this limitation plainly and specifically rather than being caught off guard — it's an obvious gap once named, so naming it yourself is stronger than being asked and hedging.",
            "follow_up_q": "What would fraud detection even look like for this kind of system?",
            "follow_up_a": "Image forensics (detecting signs of digital editing), cross-referencing extracted merchant/amount against known merchant patterns for anomalies, and — most practically for a reimbursement context — requiring the original file retained and auditable rather than just the extracted fields, none of which this project implements.",
        },
        {
            "q": "You built both a rule-based AND an ML categorizer — isn't that redundant engineering effort for a portfolio project?",
            "short": "I'd defend it as intentional, not redundant — building both let me make and document an evidence-based comparison, which is a more honest and more impressive story than only ever having one approach.",
            "detailed": "If I'd built only the rule-based system, I couldn't speak with any authority about whether ML would help here, or explain what a real precision/recall/F1 evaluation looks like for this problem. If I'd built only the ML system, I couldn't offer a transparent, zero-training-data default, and I'd be less able to explain WHY the classifier's accuracy number doesn't mean what it might look like it means. Building both, and being explicit about why one is the default, is the more complete and more honest engineering story — not redundancy for its own sake.",
            "verbal": "This is a good moment to connect back to the project's stated priority — 'engineering quality over number of features' — and explain why this specific case doesn't violate that.",
            "follow_up_q": "Was there anything you built that, in hindsight, WAS redundant or unnecessary?",
            "follow_up_a": "I don't think so within app/ itself, but I was deliberate about NOT building several things the original brief mentioned as optional — a corrections table separate from the transactions table, for instance — because the existing schema already supported the same need without adding a new table.",
        },
        {
            "q": "How do you know your synthetic sample receipts are representative enough to have caught the real bugs you found?",
            "short": "I don't know that they're fully representative — I know they caught two real, non-trivial bugs, which is evidence they're useful, not evidence they're sufficient.",
            "detailed": "The date-parsing bug and the largest-number-fallback bug were both genuinely caught by running the pipeline against synthetic receipts I generated, which is real evidence this testing approach has value. But synthetic receipts are, by construction, cleaner and more predictable than real photographed receipts — different lighting, angles, creases, and truly unpredictable layouts that a real photo would have. I'd treat 'my synthetic tests caught two real bugs' as encouraging, not as proof the extraction pipeline is robust to everything a real user would throw at it.",
            "verbal": "This is testing whether you'll overstate what your own evidence proves — resist the pull to claim more confidence than you've actually earned.",
            "follow_up_q": "What's the fastest way to get real-world validation without a large data-collection effort?",
            "follow_up_a": "Ask a handful of people (with consent) to try uploading their own real receipts and report what broke — even five or ten real photos from different phones/lighting conditions would surface failure modes synthetic data can't, at very low cost.",
        },
    ],
}
